// 🔹 Collections, Documents, and Fields
// Collection → Like a folder (example: users).
// Document → Like a file inside the folder (example: { name: "Ali", age: 25 }).
// Field → Like a property inside a document (example: name, age).